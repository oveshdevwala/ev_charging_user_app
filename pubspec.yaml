name: ev_charging_user_app
description: "EV Charging Multi-App Flutter Template - User, Station Owner, Admin"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5

  # Navigation
  go_router: ^14.6.2

  # UI/Responsive
  flutter_screenutil: ^5.9.3
  cached_network_image: ^3.4.1
  flutter_svg: ^2.0.10+1
  shimmer: ^3.0.0

  # Local Storage
  sqflite: ^2.4.1
  path: ^1.9.0
  shared_preferences: ^2.3.3

  # Utilities
  intl: ^0.20.2
  get_it: ^8.0.2
  injectable: ^2.5.0
  logger: ^2.4.0
  timeago: ^3.7.0
  uuid: ^4.5.1
  dio: ^5.4.0
  flutter_dotenv: ^5.1.0

  # Image Processing
  image_picker: ^1.1.2
  image: ^4.5.3
  path_provider: ^2.1.4

  # Icons
  cupertino_icons: ^1.0.8
  iconsax: ^0.0.8
  fl_chart: ^1.1.1
  
  # URL Launcher
  url_launcher: ^6.3.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  printing: ^5.14.2
  pdf: ^3.11.3
  json_annotation: ^4.9.0
  qr_flutter: ^4.1.0
  flutter_markdown: ^0.6.18
  crypto: ^3.0.5

  # Map & Location
  flutter_map: ^7.0.2
  flutter_map_marker_cluster: ^1.1.0
  latlong2: ^0.9.1
  geolocator: ^13.0.1
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  injectable_generator: ^2.6.2
  json_serializable: ^6.11.2

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/lottie/
    - .env


flutter_intl:
  enabled: true
  class_name: S
  main_locale: en
  arb_dir: lib/l10n
  output_dir: lib/generated
